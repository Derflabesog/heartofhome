rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lock down all public access. Cloud Functions will bypass this.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}